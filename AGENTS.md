# Communication preferences

- UI・UXの要素や操作を説明するときは、一般的な日本語の用語を積極的に伝える。
- 役立つ場合は英語名も短く併記する。例: 赤ポチ → 通知ドット（notification dot）／通知バッジ（notification badge）。
- 専門用語だけで説明せず、現在の作業との関係が分かる短い補足を付ける。
- このリポジトリの更新と確認が完了したら、関連する変更だけをコミットし、現在のブランチを `origin` へプッシュする。コミットやプッシュの依頼を改めて待たない。
- このリポジトリでコミットを作成するときは、毎回 `version.json` と `src/index.template.html` のバージョン情報を同じ新しい一意の値へ更新し、生成した `index.html` とともにそのコミットへ含める。形式は `YYYYMMDD-short-description` を基本とする。
- ユーザーの未関連な変更はコミットに含めず、プッシュできなかった場合は理由を報告する。

# Performance priorities

- 新しいチャットを含むすべての実装で、画面の操作感を保ちながら動作の軽さを最優先する。
- 操作のたびに全履歴を読み込む、全データを走査する、計算結果が変わらない処理を繰り返す、配置図や画面全体を再描画するといった実装を避ける。必要な範囲だけの差分更新、索引・集計値・キャッシュの再利用、遅延読み込みを優先し、元データ変更時の無効化も必ず行う。
- 実装後は、対象機能の正しさだけでなく、頻繁に使う操作で不要な読込・再計算・再描画が増えていないことも確認する。

# Implementation guardrails

- 過去に保存された収穫記録・部分収穫記録・苗植え記録との後方互換性を守る。データ形式や判定方法を変える場合は、既存データでも同じ意味と結果を維持できることを確認する。
- 同じ情報を複数の状態へ別々に保持せず、信頼できる元データ（single source of truth）を一つに定め、表示・集計・シミュ結果はそこから導出する。派生状態を保存する場合は同期方法と無効化条件を明確にする。
- 依頼された目的に必要な範囲だけを変更し、無関係な画面・計算・データ形式へ変更を広げない。既存の正常な挙動は可能な限り維持する。
- 選択、全選択、解除、キャンセル、保存、修正などの操作は、特別な理由がなければ既存画面と同じ操作方法・用語・配置にそろえる。
- 過去記録を編集・削除する機能では、その記録以降の収穫可能判定、苗植え状態、集計、シミュ結果への影響を確認し、必要な範囲だけを正しい順序で再計算する。
- 表示のための丸めと内部計算を分離する。整数や小数第一位で表示する場合も、明示的な仕様がない限り元の計算精度を保持する。
- 外部送信より先に端末内の記録を安全に保存し、通信失敗時は未送信として再送できる状態を維持する。送信失敗を記録保存の失敗として扱わない。
- UI変更は320px・390px幅を優先して確認し、固定バー、ボタン、入力欄、配置図が重ならず、主要操作が画面外へ押し出されないことを確認する。必要に応じて広い画面幅も確認する。
