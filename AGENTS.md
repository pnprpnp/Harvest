# Communication preferences

- UI・UXの要素や操作を説明するときは、一般的な日本語の用語を積極的に伝える。
- 役立つ場合は英語名も短く併記する。例: 赤ポチ → 通知ドット（notification dot）／通知バッジ（notification badge）。
- 専門用語だけで説明せず、現在の作業との関係が分かる短い補足を付ける。
- UI・UX以外の変更は、更新と確認が完了したら関連する変更だけをコミットし、現在のブランチを `origin` へプッシュする。コミットやプッシュの依頼を改めて待たない。
- UI・UX関連の変更は、先にブラウザで確認できるプレビューへ反映し、ユーザーと見た目・操作を調整する。プレビュー調整中は自動でコミット・プッシュしない。最終案が決まった時点でコミット・プッシュしてよいかユーザーへ確認し、許可を得てから実行する。この例外は新しいチャットでも適用する。
- UI・UXのプレビューをユーザーへ提示するときは、HTML・CSS・差分などのコードを見せるのではなく、ブラウザで操作できる実画面プレビューを用意する。この希望は新しいチャットでも引き継ぐ。
- このリポジトリでコミットを作成するときは、毎回 `version.json` と `src/index.template.html` のバージョン情報を同じ新しい一意の値へ更新し、生成した `index.html` とともにそのコミットへ含める。形式は `YYYYMMDD-short-description` を基本とする。
- ユーザーの未関連な変更はコミットに含めず、プッシュできなかった場合は理由を報告する。

# Performance priorities

- 新しいチャットを含むすべての実装で、画面の操作感を保ちながら動作の軽さを最優先する。
- 操作のたびに全履歴を読み込む、全データを走査する、計算結果が変わらない処理を繰り返す、配置図や画面全体を再描画するといった実装を避ける。必要な範囲だけの差分更新、索引・集計値・キャッシュの再利用、遅延読み込みを優先し、元データ変更時の無効化も必ず行う。
- 実装後は、対象機能の正しさだけでなく、頻繁に使う操作で不要な読込・再計算・再描画が増えていないことも確認する。

# Commercial-use and cost priorities

- このアプリは商用の営農業務で利用する前提とし、新しいチャットを含むすべての実装で優先事項として扱う。
- 外部API、データ、ライブラリ、その他のサービスは、想定する利用方法で商用利用が明示的に認められ、継続的な利用料金が発生しないものを優先する。
- 非商用限定の無料枠、期限付きの無料試用、初回クレジットだけで無料になるサービスは、本番機能の依存先にしない。
- 商用利用条件が不明確な場合、無料枠を超える可能性がある場合、または有料化しないと必要機能を満たせない場合は、実装前にユーザーへ条件と代替案を説明して確認する。
- 無料利用に出典表示、加工表示、呼出回数、保存期間などの条件がある場合は、実装と画面表示の両方で条件を満たす。規約や料金体系は変更され得るため、外部サービスの新規採用・切替時には公式情報を再確認する。
- 気象データを使う機能では、商用利用不可の Open-Meteo 無料ホストAPIを本番の依存先にしない。商用利用可能かつ無料の公的データを優先し、必要に応じて端末内または既存の保存先へ最小限のデータを蓄積・再利用する。

# Code-reading and usage efficiency

- 作業開始時は、リポジトリ全体を読み直す前に `docs/CODEBASE_MAP.md` を読み、記載された確認基準コミット以降の差分を `git diff --name-only` で確認する。
- 変更対象と直接の依存先だけを `rg` と必要な行範囲で調べ、生成ファイルの `index.html` と `apps-script/コード.js` は通常の調査・編集対象にしない。
- コードマップと実装が食い違う場合は実装を正として扱い、担当範囲、主要経路、データの信頼できる元、または必須確認手順が変わった場合だけコードマップを更新する。
- 調査や検証は対象機能に近い小さい確認から始め、必要な場合だけ全特性テストへ広げる。
- 完了報告は結論、変更ファイル、確認結果、未解決事項に絞り、長いツール出力や既知の説明を繰り返さない。

# Implementation guardrails

- 過去に保存された収穫記録・部分収穫記録・苗植え記録との後方互換性を守る。データ形式や判定方法を変える場合は、既存データでも同じ意味と結果を維持できることを確認する。
- 同じ情報を複数の状態へ別々に保持せず、信頼できる元データ（single source of truth）を一つに定め、表示・集計・シミュ結果はそこから導出する。派生状態を保存する場合は同期方法と無効化条件を明確にする。
- 依頼された目的に必要な範囲だけを変更し、無関係な画面・計算・データ形式へ変更を広げない。既存の正常な挙動は可能な限り維持する。
- 選択、全選択、解除、キャンセル、保存、修正などの操作は、特別な理由がなければ既存画面と同じ操作方法・用語・配置にそろえる。
- 過去記録を編集・削除する機能では、その記録以降の収穫可能判定、苗植え状態、集計、シミュ結果への影響を確認し、必要な範囲だけを正しい順序で再計算する。
- 表示のための丸めと内部計算を分離する。整数や小数第一位で表示する場合も、明示的な仕様がない限り元の計算精度を保持する。
- 外部送信より先に端末内の記録を安全に保存し、通信失敗時は未送信として再送できる状態を維持する。送信失敗を記録保存の失敗として扱わない。
- UI変更は320px・390px幅を優先して確認し、固定バー、ボタン、入力欄、配置図が重ならず、主要操作が画面外へ押し出されないことを確認する。必要に応じて広い画面幅も確認する。
