# Working rules

## Communication and delivery

- UI・UXは一般的な日本語で説明し、役立つ場合だけ英語名を併記する。専門用語には作業との関係が分かる短い補足を付ける。
- 依頼範囲だけを変更し、ユーザーの未関連な変更は編集・コミットしない。
- UI・UX以外は、実装と確認後に関連変更だけをコミットし、現在のブランチを `origin` へプッシュする。
- UI・UXは先に実装へ反映し、コードやモックではなくブラウザで操作できる実画面プレビューを用意する。320px・390px幅を優先確認し、最終案の了承後にコミット・プッシュの許可を得る。
- コミットごとに `version.json` と `src/index.template.html` を同じ一意の版（基本形 `YYYYMMDD-short-description`）へ更新し、生成した `index.html` も含める。プッシュ失敗時は理由を報告する。

## Product priorities

- 商用の営農業務で使うアプリとして、正しさと操作感を保ちながら軽さを優先する。
- 操作ごとの全履歴読込み・全件走査・不変な再計算・画面全体の再描画を避け、差分更新、索引、キャッシュ、遅延読込みを使う。元データ変更時は関連する派生状態だけを無効化する。
- 外部API・データ・ライブラリは、想定用途で商用利用が明示され、継続料金のないものを優先する。条件が不明、有料化が必要、無料上限を超え得る場合は、条件と代替案を実装前に確認する。
- 無料利用の出典・加工表示・回数・保存期間などの条件を実装と表示で守る。外部サービスの採用・切替時は公式の最新規約を確認する。
- 気象データは、商用利用不可の Open-Meteo 無料ホストAPIへ本番依存しない。商用利用可能で無料の公的データを優先し、必要最小限を端末内または既存の保存先で再利用する。

## Implementation safeguards

- 過去の収穫・部分収穫・苗植え記録との互換性を守る。形式や判定を変える場合は既存データでも意味と結果を維持する。
- 信頼できる元データ（single source of truth）を一つにし、表示・集計・シミュ結果をそこから導出する。派生状態には同期方法と無効化条件を持たせる。
- 既存の正常動作を維持し、選択、解除、キャンセル、保存、修正などは既存画面の用語・配置・操作へそろえる。
- 過去記録の編集・削除では、対象以降の収穫可能判定、苗植え状態、集計、シミュ結果への影響を確認し、必要な範囲だけ順序どおり再計算する。
- 表示用の丸めと内部計算を分離し、明示仕様がない限り元の精度を保持する。
- 外部送信より先に端末へ安全に保存し、通信失敗は未送信として再送可能にする。送信失敗を記録保存の失敗として扱わない。

## Repository workflow

- 機能横断、主要データ経路、または不慣れな領域の変更では `docs/CODEBASE_MAP.md` を索引として使う。局所修正では対象と直接の依存先だけを `rg` と必要な行範囲で調べる。
- `index.html` と `apps-script/コード.js` は直接編集せず、分割ソースから生成する。
- コードマップは、担当範囲、主要経路、信頼できる元データ、必須確認手順が変わった場合だけ更新する。食い違う場合は実装を正とする。
- 対象に近い小さい確認から始め、変更リスクに応じて確認範囲を広げる。頻繁な操作で不要な読込み・再計算・再描画が増えていないことも確認する。
- 完了報告は結論、変更ファイル、確認結果、未解決事項に絞る。
