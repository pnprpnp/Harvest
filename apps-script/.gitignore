# Apps Script source is temporarily kept local because the current source
# contains a fixed Drive resource ID. Remove this guard only after that value
# has been moved to Script Properties.
*
!.gitignore
!.claspignore
!README.md
