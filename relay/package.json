{
  "name": "harvestnavi-record-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply harvestnavi-record-relay --local",
    "migrate:remote": "wrangler d1 migrations apply harvestnavi-record-relay --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
